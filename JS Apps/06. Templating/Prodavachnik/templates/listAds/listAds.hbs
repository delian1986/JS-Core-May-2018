{{>header}}

<section id="viewAds" class="viewAds">
    <h1 class="titleForm">Advertisements</h1>
    <div id="ads" class="ads">
        {{#each ads}}
            {{>adBox}}
        {{else}}
            <p>There is no ads in database.</p>
        {{/each}}
    </div>
</section>

{{>footer}}