{{>header}}
<div class="content">
{{>navigation}}
    <section id="viewCatalog">
        <div class="posts">
            {{#each posts}}
                {{>post}}
            {{else}}
                <p>There are no posts in database</p>
            {{/each}}
        </div>
    </section>

</div>
{{>footer}}